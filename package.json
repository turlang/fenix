{
  "name": "mestre-orc-engine",
  "version": "0.1.0-alpha.51",
  "description": "Motor modular de narração cinematográfica com simulação automatizada de sessões, testes de carga, Central Mestre Orc, diagnóstico, backups, automações, tutores, mapas, memória, combate, voz neural e IA resiliente.",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node apps/api/src/server.js",
    "start": "node apps/api/src/server.js",
    "test": "node scripts/run-tests.mjs",
    "audit": "npm audit --omit=dev",
    "validate": "node scripts/validate.mjs",
    "check:offline": "npm run validate && npm run test:all",
    "release:prepare": "node scripts/prepare-release.mjs",
    "check": "npm run validate && npm run audit && npm run test:all",
    "test:integration": "node scripts/run-integration-tests.mjs",
    "test:session": "node scripts/run-session-simulation.mjs",
    "test:load": "node scripts/run-load-tests.mjs",
    "test:all": "npm test && npm run test:integration && npm run test:session && npm run test:load"
  },
  "engines": {
    "node": ">=20 <25"
  },
  "license": "UNLICENSED",
  "overrides": {
    "@fastify/ajv-compiler": {
      "fast-uri": "3.1.4"
    },
    "ajv": {
      "fast-uri": "3.1.4"
    }
  },
  "dependencies": {
    "fastify": "^5.2.1"
  }
}
