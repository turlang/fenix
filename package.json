{
  "name": "mestre-orc-engine",
  "version": "1.0.0-rc.3",
  "description": "Release Candidate do Mestre Orc Engine: narração, voz, combate, memória, criação assistida, segurança, diagnóstico, backup, migrações e distribuição para Foundry VTT 13.",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node apps/api/src/server.js",
    "start": "node apps/api/src/server.js",
    "test": "node scripts/run-tests.mjs",
    "audit": "npm audit --omit=dev",
    "validate": "node scripts/validate.mjs",
    "check:offline": "npm run validate && npm run test:all && npm run rc:audit",
    "release:prepare": "node scripts/prepare-release.mjs",
    "check": "npm run validate && npm run audit && npm run test:all && npm run rc:audit",
    "test:integration": "node scripts/run-integration-tests.mjs",
    "test:session": "node scripts/run-session-simulation.mjs",
    "test:load": "node scripts/run-load-tests.mjs",
    "test:all": "npm test && npm run test:integration && npm run test:session && npm run test:load",
    "migrate:inspect": "node scripts/migrate-data.mjs inspect",
    "migrate:apply": "node scripts/migrate-data.mjs apply",
    "install:verify": "node scripts/verify-installation.mjs",
    "release:build": "node scripts/build-distribution.mjs",
    "rc:audit": "node scripts/release-candidate-audit.mjs",
    "sbom:generate": "node scripts/generate-sbom.mjs",
    "release:rc": "npm run check && npm run release:build && npm run install:verify"
  },
  "engines": {
    "node": ">=20 <25"
  },
  "license": "UNLICENSED",
  "overrides": {
    "@fastify/ajv-compiler": {
      "fast-uri": "3.1.4"
    },
    "ajv": {
      "fast-uri": "3.1.4"
    }
  },
  "dependencies": {
    "fastify": "5.10.0"
  }
}
